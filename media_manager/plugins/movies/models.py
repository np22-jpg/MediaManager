# Movies Plugin Models
# For now, we'll import the existing models to maintain compatibility
# Future versions can migrate to plugin-based models

from media_manager.movies.models import (
    Movie,
    MovieFile,
    MovieRequest
)